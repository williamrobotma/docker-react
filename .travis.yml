language: generic

sudo: required
services:
  - docker


before_install:
  - docker build -t williamma/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true williamma/docker-react npm run test
